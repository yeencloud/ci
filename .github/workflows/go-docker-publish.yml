name: "Go Test, Lint, and Docker Publish"

on:
  workflow_call:

jobs:
  test-and-lint:
    name: Test and Lint
    runs-on: ubuntu-latest

    permissions:
      contents: read  # needed to checkout code (including private) & read modules

    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          persist-credentials: false

      - name: Lowercase org name
        id: org_lowercase
        uses: ASzc/change-string-case-action@v3.0.1
        with:
          input: ${{ github.repository_owner }}
          transform: toLowerCase

      - name: Authenticate Git for private modules
        run: |
          echo "Configuring git for private modules..."
          git config --global url."https://${{ secrets.GITHUB_TOKEN }}@github.com/".insteadOf "https://github.com/"
          ORG_NAME_LOWER="${{ steps.org_lowercase.outputs.value }}"
          go env -w GOPRIVATE=github.com/$ORG_NAME_LOWER/*

      - name: Set up Go (latest 1.x)
        uses: actions/setup-go@v3
        with:
          go-version: "1.x"

      - name: Lint and Test
        uses: AliceDiNunno/action-go-test-and-lint@v0.3.0

  build-and-publish:
    name: Build & Publish
    runs-on: ubuntu-latest
    needs: [test-and-lint]  # Only runs after tests/lint pass

    permissions:
      contents: read
      packages: write  # needed for pushing Docker images to GHCR

    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          persist-credentials: false

      - name: Lowercase org name
        id: org_lowercase
        uses: ASzc/change-string-case-action@v3.0.1
        with:
          input: ${{ github.repository_owner }}
          transform: toLowerCase

      - name: Lowercase repo name
        id: repo_lowercase
        uses: ASzc/change-string-case-action@v3.0.1
        with:
          input: ${{ github.event.repository.name }}
          transform: toLowerCase

      - name: Authenticate Git for private modules
        run: |
          echo "Configuring git for private modules..."
          git config --global url."https://${{ secrets.GITHUB_TOKEN }}@github.com/".insteadOf "https://github.com/"
          ORG_NAME_LOWER="${{ steps.org_lowercase.outputs.value }}"
          go env -w GOPRIVATE=github.com/$ORG_NAME_LOWER/*

      - name: Set up Go (latest 1.x)
        uses: actions/setup-go@v3
        with:
          go-version: "1.x"

      - name: Log in to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Set up QEMU (for cross-building)
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and push multi-arch Docker image
        run: |
          ORG_NAME_LOWER="${{ steps.org_lowercase.outputs.value }}"
          REPO_NAME_LOWER="${{ steps.repo_lowercase.outputs.value }}"

          docker buildx build \
            --platform linux/amd64,linux/arm/v7 \
            -t ghcr.io/${ORG_NAME_LOWER}/${REPO_NAME_LOWER}:latest \
            --push .
